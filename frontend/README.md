# Frontend Backend-Test Harness

Minimal Next.js 16 frontend used to test backend endpoints and flows.

## Run

```bash
pnpm dev
```

Open `http://localhost:3000`.

## Notes

- Main entry: `app/page.tsx`
- Global styles: `app/globals.css`
- Keep this frontend intentionally minimal while backend contracts are being tested.
